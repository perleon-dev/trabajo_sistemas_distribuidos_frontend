import { AmoutFormatPipe } from './amout-format.pipe';

describe('AmoutFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AmoutFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
